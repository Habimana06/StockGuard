server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Static React app only — API is called from the browser at VITE_API_URL (port 4000)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
